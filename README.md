# MIG — Mirror Interface Guidelines

Design guidelines for interactive half-mirror ambient displays.
Version 0.1.0, DRAFT (provisional, not ratified).

- Human-readable site: built from `docs/` by `site-build/build.py`
  (`python3 site-build/build.py` → `site/`), deployed to GitHub Pages.
- Source of truth is `docs/*.md`. Status discipline (borrowed / derived /
  proposed) is enforced by convention — see the site's validation page.
